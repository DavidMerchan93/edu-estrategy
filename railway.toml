[build]
builder = "nixpacks"
buildCommand = "npm install --prefix backend-edustrategy"

[deploy]
startCommand = "node backend-edustrategy/servidor.js"
healthcheckPath = "/api/salud"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
